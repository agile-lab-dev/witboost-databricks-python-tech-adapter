apiVersion: v1
kind: Service
metadata:
  name: {{ template "pythondatabricksta.name" . }}
  labels:
{{- include "pythondatabricksta.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5002
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "pythondatabricksta.name" . }}
